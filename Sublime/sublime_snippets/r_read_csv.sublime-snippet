<snippet>
	<content><![CDATA[
${1:library(readr)}
${2:df_data} <- read_delim(file='./data/${3:file_name}.csv', delim=',')
${2:df_data} <- data.frame(${2:df_data}) # read_delim returns a tibbl, may or may not want to keep this.
str(${2:df_data})


]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>r_read_csv</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
